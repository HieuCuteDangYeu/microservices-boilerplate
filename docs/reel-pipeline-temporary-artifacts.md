# Reel pipeline temporary artifacts

This manifest tracks artifacts introduced by the media, queue, and semantic
indexing upgrade. Phase 10 may remove an artifact only after its purpose has
been covered by local, integration, and production canary validation.

| Path                                                                                     | Phase | Purpose                                                                                                                       | Package script using it       | Classification       | Final removal decision                                                             |
| ---------------------------------------------------------------------------------------- | ----- | ----------------------------------------------------------------------------------------------------------------------------- | ----------------------------- | -------------------- | ---------------------------------------------------------------------------------- |
| `scripts/tmp/load-test-reel-pipeline.cjs`                                                | 0     | Upload fixtures through the real Media and Content APIs, poll Reel status, and report concurrent latency percentiles.         | `tmp:load-test:reel-pipeline` | Temporary diagnostic | Remove in Phase 10 after the production canary covers upload and polling behavior. |
| `scripts/tmp/inspect-media-output.cjs`                                                   | 0     | Compare source and HLS stream geometry, inspect local HLS size/object counts, and detect landscape-to-portrait crop evidence. | `tmp:inspect:media-output`    | Temporary diagnostic | Remove in Phase 10 after no-crop production canary validation.                     |
| `scripts/tmp/load-test-reel-pipeline.spec.ts`                                            | 0     | Verify percentile math, configuration parsing, absolute fixture enforcement, and polling timeout.                             | `tmp:test:reel-pipeline`      | Temporary test       | Remove in Phase 10 after production validation.                                    |
| `apps/processing-service/src/application/use-cases/classify-reel-media.use-case.spec.ts` | 0     | Verify portrait, landscape, square, rotation-aware orientation, and the short/long metric boundary.                           | `tmp:test:reel-pipeline`      | Temporary test       | Remove in Phase 10 after production validation.                                    |
| `apps/processing-service/src/infrastructure/services/processing-metrics.service.spec.ts` | 0     | Verify stage duration and failure-stage recording.                                                                            | `tmp:test:reel-pipeline`      | Temporary test       | Remove in Phase 10 after production validation.                                    |
