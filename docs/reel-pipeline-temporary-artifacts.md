# Reel pipeline temporary artifacts

This manifest records artifacts introduced by the media, queue, and semantic
indexing upgrade. Phase 9 production validation covered their purposes, and
Phase 10 removed every listed artifact in cleanup commit `6b8eb30`.

| Path                                                                                                         | Phase | Purpose                                                                                                                            | Package script using it          | Classification       | Final removal decision                                                             |
| ------------------------------------------------------------------------------------------------------------ | ----- | ---------------------------------------------------------------------------------------------------------------------------------- | -------------------------------- | -------------------- | ---------------------------------------------------------------------------------- |
| `scripts/tmp/load-test-reel-pipeline.cjs`                                                                    | 0     | Upload fixtures through the real Media and Content APIs, poll Reel status, and report concurrent latency percentiles.              | `tmp:load-test:reel-pipeline`    | Temporary diagnostic | Removed in `6b8eb30` after Phase 9 production validation. |
| `scripts/tmp/inspect-media-output.cjs`                                                                       | 0     | Compare source and HLS stream geometry, inspect local HLS size/object counts, and detect landscape-to-portrait crop evidence.      | `tmp:inspect:media-output`       | Temporary diagnostic | Removed in `6b8eb30` after Phase 9 production validation. |
| `scripts/tmp/load-test-reel-pipeline.spec.ts`                                                                | 0     | Verify percentile math, configuration parsing, absolute fixture enforcement, and polling timeout.                                  | `tmp:test:reel-pipeline`         | Temporary test       | Removed in `6b8eb30` after Phase 9 production validation. |
| `apps/media-processing-service/src/application/use-cases/classify-reel-media.use-case.spec.ts`               | 0     | Verify portrait, landscape, square, rotation-aware orientation, and the short/long metric boundary.                                | `tmp:test:reel-pipeline`         | Temporary test       | Removed in `6b8eb30` after Phase 9 production validation. |
| `apps/media-processing-service/src/infrastructure/services/processing-metrics.service.spec.ts`               | 0     | Verify stage duration and failure-stage recording.                                                                                 | `tmp:test:reel-pipeline`         | Temporary test       | Removed in `6b8eb30` after Phase 9 production validation. |
| `scripts/tmp/inspect-reel-queues.cjs`                                                                        | 1     | Inspect the Phase 1 media queue topology without consuming jobs.                                                                   | `tmp:inspect:reel-queues`        | Temporary diagnostic | Removed in `6b8eb30` after Phase 9 production validation. |
| `scripts/tmp/failure-test-reel-queues.cjs`                                                                   | 1     | Inject a correlation-tagged malformed job and verify dead-letter routing through a temporary observer queue.                       | `tmp:failure-test:reel-queues`   | Temporary diagnostic | Removed in `6b8eb30` after Phase 9 production validation. |
| `scripts/tmp/reel-queue-tools.spec.ts`                                                                       | 1     | Verify diagnostic queue names, non-consuming inspection, and correlation-tagged failure injection.                                 | `tmp:test:reel-queues`           | Temporary test       | Removed in `6b8eb30` after Phase 9 production validation. |
| `apps/content-service/src/application/use-cases/create-reel-outbox.use-case.spec.ts`                         | 1     | Verify creation submits the Reel and media job through one transactional repository boundary without direct publish.               | `tmp:test:reel-queues`           | Temporary test       | Removed in `6b8eb30` after Phase 9 production validation. |
| `apps/content-service/src/application/use-cases/classify-reel-job-length.use-case.spec.ts`                   | 1     | Verify conservative creation-time short, long, and unknown classification.                                                         | `tmp:test:reel-queues`           | Temporary test       | Removed in `6b8eb30` after Phase 9 production validation. |
| `apps/content-service/src/application/use-cases/dispatch-outbox-events.use-case.spec.ts`                     | 1     | Verify post-commit recovery, publish recovery, and duplicate-dispatcher claim behavior.                                            | `tmp:test:reel-queues`           | Temporary test       | Removed in `6b8eb30` after Phase 9 production validation. |
| `apps/content-service/src/application/use-cases/claim-reel-processing-attempt.use-case.spec.ts`              | 1     | Verify duplicate, stale, and redelivered media-job claim behavior.                                                                 | `tmp:test:reel-queues`           | Temporary test       | Removed in `6b8eb30` after Phase 9 production validation. |
| `apps/media-processing-service/src/application/use-cases/validate-reel-source-media.use-case.phase1.spec.ts` | 1     | Verify ffprobe-authoritative enforcement of the configurable long-video duration ceiling.                                          | `tmp:test:reel-queues`           | Temporary test       | Removed in `6b8eb30` after Phase 9 production validation. |
| `apps/media-processing-service/src/infrastructure/controllers/media-processing.controller.phase1.spec.ts`    | 1     | Verify manual acknowledgement, retry publication, requeue-before-durable-result, and DLQ behavior.                                 | `tmp:test:reel-queues`           | Temporary test       | Removed in `6b8eb30` after Phase 9 production validation. |
| `apps/content-service/src/domain/reel-status-compatibility.mapper.phase2.spec.ts`                            | 2     | Verify the legacy status mapper remains media-driven for every independent index outcome.                                          | `tmp:test:reel-state`            | Temporary test       | Removed in `6b8eb30` after Phase 9 production validation. |
| `apps/content-service/src/infrastructure/repositories/content.repository.phase2.spec.ts`                     | 2     | Verify stale media/index guards and that indexing failure cannot make completed media unplayable.                                  | `tmp:test:reel-state`            | Temporary test       | Removed in `6b8eb30` after Phase 9 production validation. |
| `apps/media-processing-service/src/application/use-cases/media-processing.phase3.spec.ts`                    | 3     | Verify the full orientation, rotation, audio, VFR, FPS, duration, validation, FFmpeg-argument, and long-audio segmentation matrix. | `tmp:test:reel-pipeline`         | Temporary test       | Removed in `6b8eb30` after Phase 9 production validation. |
| `apps/media-processing-service/src/infrastructure/services/ffmpeg.service.phase3.spec.ts`                    | 3     | Verify spawned-process exit status, bounded stderr, timeout termination, and cancellation.                                         | `tmp:test:reel-pipeline`         | Temporary test       | Removed in `6b8eb30` after Phase 9 production validation. |
| `scripts/tmp/verify-semantic-index.cjs`                                                                      | 6     | Validate pgvector version, semantic tables, stored search vectors, and required HNSW/GIN indexes without changing data.            | `tmp:verify:semantic-index`      | Temporary diagnostic | Removed in `6b8eb30` after Phase 9 production validation. |
| `scripts/tmp/explain-semantic-index.cjs`                                                                     | 6     | Capture vector and keyword query plans to confirm HNSW and GIN planner paths.                                                      | `tmp:explain:semantic-index`     | Temporary diagnostic | Removed in `6b8eb30` after Phase 9 production validation. |
| `scripts/tmp/benchmark-semantic-index.cjs`                                                                   | 6     | Measure per-level ANN latency, exact-versus-ANN Recall@K, filtered retrieval, and index size.                                      | `tmp:benchmark:semantic-index`   | Temporary diagnostic | Removed in `6b8eb30` after Phase 9 production validation. |
| `apps/ai-service/src/application/use-cases/retrieval-agent.use-case.spec.ts`                                 | 7     | Verify shared-Reel access precedes hierarchical Reel, section, and chunk retrieval and produces safe empty context.                | `tmp:test:semantic-connections`  | Temporary test       | Removed in `6b8eb30` after Phase 9 production validation. |
| `apps/content-service/src/infrastructure/adapters/semantic-recommendation-service.adapter.spec.ts`           | 7     | Verify recommendations search Reel documents only and skip the semantic source when the Indexing Service is unavailable.           | `tmp:test:semantic-connections`  | Temporary test       | Removed in `6b8eb30` after Phase 9 production validation. |
| `apps/content-service/src/infrastructure/repositories/content.repository.phase4.spec.ts`                     | 4     | Verify guarded media completion and the exact indexing outbox handoff are committed atomically.                                    | `tmp:test:reel-indexing`         | Temporary test       | Removed in `6b8eb30` after Phase 9 production validation. |
| `apps/reel-indexing-service/src/application/use-cases/reel-indexing.phase4.spec.ts`                          | 4     | Verify segmented transcription resume, ordered merge, missing-segment detection, and bounded long-video work.                      | `tmp:test:reel-indexing`         | Temporary test       | Removed in `6b8eb30` after Phase 9 production validation. |
| `apps/ai-service/src/application/use-cases/generate-embedding-batch.use-case.spec.ts`                        | 5     | Verify bounded embedding concurrency and stable batch result ordering.                                                             | `tmp:test:reel-indexing`         | Temporary test       | Removed in `6b8eb30` after Phase 9 production validation. |
| `apps/content-service/src/infrastructure/repositories/content.repository.phase5.spec.ts`                     | 5     | Verify reindex-only operations preserve completed playable media and enqueue a fresh index attempt.                                | `tmp:test:reel-indexing`         | Temporary test       | Removed in `6b8eb30` after Phase 9 production validation. |
| `apps/reel-indexing-service/src/application/use-cases/reel-indexing.phase5.spec.ts`                          | 5     | Verify hierarchical section/chunk generation, batching, cache reuse, and bounded concurrency.                                      | `tmp:test:reel-indexing`         | Temporary test       | Removed in `6b8eb30` after Phase 9 production validation. |
| `scripts/tmp/benchmark-reel-indexing.cjs`                                                                    | 5     | Measure deterministic long-video hierarchy construction size, batching, and elapsed time without external writes.                  | `tmp:benchmark:indexing`         | Temporary diagnostic | Removed in `6b8eb30` after Phase 9 production validation. |
| `apps/reel-indexing-service/src/application/use-cases/reel-indexing.phase6.spec.ts`                          | 6     | Verify candidate activation, idempotent completion, and failure cleanup around semantic indexing.                                  | `tmp:test:reel-indexing`         | Temporary test       | Removed in `6b8eb30` after Phase 9 production validation. |
| `apps/reel-indexing-service/src/infrastructure/repositories/prisma-semantic-index.repository.spec.ts`        | 6     | Verify semantic repository persistence, activation, deletion, and hybrid-search query construction.                                | `tmp:test:reel-indexing`         | Temporary test       | Removed in `6b8eb30` after Phase 9 production validation. |
| `scripts/tmp/seed-phase8-semantic-index.cjs`                                                                 | 8     | Seed and remove an isolated deterministic long-video hierarchy for real pgvector and filtered retrieval validation.                | `tmp:seed:phase8-semantic-index` | Temporary diagnostic | Removed in `6b8eb30` after Phase 9 production validation. |
| `scripts/tmp/phase8-auth-stub.cjs`                                                                           | 8     | Serve one fixed test identity only on a loopback RabbitMQ broker so isolated API upload validation needs no real account.          | `tmp:serve:phase8-auth-stub`     | Temporary diagnostic | Removed in `6b8eb30` after Phase 9 production validation. |
| `scripts/tmp/phase8-ai-stub.cjs`                                                                             | 8     | Return deterministic AI responses or immediate unavailable errors only on a loopback broker for isolated indexing validation.      | `tmp:serve:phase8-ai-stub`       | Temporary diagnostic | Removed in `6b8eb30` after Phase 9 production validation. |
| `apps/ai-service/src/infrastructure/adapters/cloudflare-transcription.adapter.spec.ts`                       | 9     | Verify successful empty transcription responses and structured provider RPC error messages found by the production canary.         | None                             | Temporary test       | Removed in `6b8eb30` after Phase 9 production validation. |

The Phase 10 validation record is
`docs/reel-pipeline-phase-10-cleanup.md`. No test-only fixture, helper, or Jest
configuration created by the upgrade remained after the cleanup commit.
